{
  "name": "oneprint",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n \"SERVER,CLIENT\" -c \"cyan,magenta\" \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "start": "npm run dev",
    "setup": "npm install && npm --prefix server install && npm --prefix client install && npm --prefix server run db:generate",
    "db:push": "npm --prefix server run db:push",
    "db:studio": "npm --prefix server run db:studio"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
